# Condicionales y Métodos

- Recapitular brevemente `if`/`else`
- `while(true)`
- Funciones
  - DRY
  - organizar la implementación
  - reusar código
- `switch`
  - similitudes y diferencias con `if`/`else`
- `enum`
  - acoplamiento explícito
  - ventajas sobre `string`
- variables estáticas
  - cómo mantener estado en la aplicación